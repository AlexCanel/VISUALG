Algoritmo "tabuada"

Var

 numero, i, x : inteiro


Inicio

      escreva("Digite um nume inteiro: ")
      leia(numero)

      i <- 0

      para i de 1 ate 10 faca
      x  <- numero * i
      escreval(numero, " x ",i," = ", x)
      i <- i + 1
      x <- 0
      fimpara

Fimalgoritmo

Ler um número inteiro N, daí mostrar na tela a tabuada de N para 1 a 10, conforme exemplo.
